from django.apps import AppConfig


class ContactConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'Contact'
    verbose_name = 'مدیریت ارتباط با من'
    verbose_name_plural = 'مدیریت ارتباط با من '
